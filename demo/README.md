# Live-code

1. `maturin new`
2. `uv venv init`
3. code `to_html(str, bool) -> str`
4. `maturin develop`
5. python `import md; md.to_html('**plop**', False)`
6. udpate code `println!("plop")`
7. test -> penible -> `uv add maturin_import_hook`
8. docstring + signature -> test dans repl
